import { CategorieNomPipe } from './categorie-nom.pipe';

describe('CategorieNomPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieNomPipe();
    expect(pipe).toBeTruthy();
  });
});
